import React from "react";

const LandingPage = () => {
  return (
    <div>
      <h1>Students</h1>
      <table>
        {/* Table rows that can be clicked to open student modal */}
      </table>

      <button>Add New Student</button>

      <select>{/* Dropdown to switch roles */}</select>
    </div>
  );
};

export default LandingPage;
